import 'package:win_plugin_link_repair/win_plugin_link_repair.dart';

Future<void> main() async {
  // Equivalent to running `dart run win_plugin_link_repair --dry-run`.
  await run(<String>['--dry-run']);
}
